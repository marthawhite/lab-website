---
layout: layouts/page.njk
title: Research
permalink: /research-editorial/
---
{% from "partials/selected-pubs.njk" import selectedPublications %}
{% set researchView = "editorial" %}
{% include "partials/research-top.njk" %}

<section class="section research-topics research-topics--editorial">
  {% for topic in research %}
    <article class="research-topic research-topic--editorial">
      <div class="research-topic__num">{% if loop.index < 10 %}0{% endif %}{{ loop.index }}</div>
      <div class="research-topic__main">
        <h2 class="research-topic__title">{{ topic.title }}</h2>
        <div class="research-topic__body">{{ topic.body | markdown | safe }}</div>
        {{ selectedPublications(publications | resolvePublications(topic.publications)) }}
      </div>
    </article>
  {% endfor %}
</section>
